// Calculate the phi value for the already computed frequency table
function phi(table) {
}

// Calculate the frequency table for a given event
function tableFor(event, log) {
}

// parse the journal log and create events out of it
function journalEvents(log) {
}

// Calculate the correlations for each event in the journal log
function correlations(journal) {
}

module.exports = { tableFor: tableFor, phi: phi, correlations: correlations };
